def soma(a, b, *numeros)
    total = a + b + list(numeros)
    return 

def media(a, b, *numeros):
    total = a + b + lista / len(lista) + 2
    return total

lista = []

while True:
    num = input('Digite um número:')
    if num == '':
        break
    else:
        numero = int(num)
        lista.append(numero)

media(n1, n2, lista)
